# Laravel Template

## Setup
```bash
cp .env.example .env
```

Set the database settings in the .env file.
Replace all instances of "laravel_template" with the project name. 

```bash
composer i
npm i
php artisan migrate
```


## Run
```bash
npm run dev
```